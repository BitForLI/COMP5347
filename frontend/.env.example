# 构建/开发时注入；必须以 VITE_ 开头才会暴露给前端代码
# 本地默认可不建 .env（代码里已 fallback 到 localhost:4000）
VITE_API_BASE_URL=http://localhost:4000/api

# 线上示例（部署 Cloudflare Pages / 本地打生产包前改成你的 Render 地址）：
# VITE_API_BASE_URL=https://你的服务.onrender.com/api

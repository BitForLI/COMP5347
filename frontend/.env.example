# Build-time vars for Vite (must start with VITE_)
# Omit file entirely to fall back to http://localhost:4000/api in code.
VITE_API_BASE_URL=http://localhost:4000/api

# Production API example (must end with /api):
# VITE_API_BASE_URL=https://your-service.onrender.com/api
